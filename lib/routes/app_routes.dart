import 'package:get/get.dart';

abstract class Routes {
  static const splash = '/';
  static const home = '/home';
  static const profile = '/profile';
  static const calendar = '/calendar';
}

// Optional middlewares or bindings can be added later using GetX features.
